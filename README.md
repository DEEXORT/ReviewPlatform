### Файл `README.md`

```markdown
# ⚡ Terminal Review Platform

![Java](https://img.shields.io/badge/Java-21-orange?style=for-the-badge&logo=openjdk)
![Spring Boot](https://img.shields.io/badge/Spring_Boot-3.5.9-green?style=for-the-badge&logo=springboot)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue?style=for-the-badge&logo=postgresql)
![Docker](https://img.shields.io/badge/Docker-Enabled-blue?style=for-the-badge&logo=docker)

Платформа для обмена данными и отзывами о технологиях, играх и медиа-контенте, выполненная в эстетике киберпанк-терминала.

---

## 🚀 Быстрый запуск (Docker)

Для запуска всей системы (Приложение + База данных) одной командой:

1. Убедитесь, что у вас установлен **Docker** и **Docker Compose**.
2. Выполните команду в корне проекта:
   ```bash
   docker-compose up -d --build

```

3. Дождитесь инициализации логов. Система будет доступна по адресу: `http://localhost:8080`

---

## 🛠 Технологический стек

* **Core:** Java 21, Spring Boot 3.5
* **Security:** Spring Security (JWT / Session based)
* **Database:** PostgreSQL + Liquibase (миграции данных)
* **ORM:** Spring Data JPA (Hibernate)
* **View:** Thymeleaf + Bootstrap 5 (Custom Cyberpunk CSS)
* **Tools:** Lombok, MapStruct, Docker

---

## 📂 Структура данных

Система автоматически инициализирует следующие узлы данных при первом запуске:

* **Users:** Роли `USER` и `ADMIN`.
* **Categories:** Frameworks, Infrastructure, Video Games, и др.
* **Products:** Hibernate, Spring Security, Docker, Kafka и др.
* **Reviews:** Пользовательские логи с рейтингом и описанием.

---

## 💻 Функциональные возможности

* [x] **Terminal UI:** Интерфейс с эффектом печатающей машинки и глитч-эффектами.
* [x] **CRUD Операции:** Создание, редактирование и удаление отзывов.
* [x] **Role Access:** Разграничение прав доступа (авторство/администрирование).
* [x] **Data Integrity:** Автоматический пересчет рейтинга и счетчиков отзывов.
* [x] **Error Handling:** Кастомные страницы ошибок (404/5xx) в стиле System Failure.

---

## 🔧 Локальная разработка

Если вы хотите запустить проект без Docker:

1. Установите локально **PostgreSQL** и создайте БД `project`.
2. Настройте `application.yml` (логин/пароль к вашей БД).
3. Соберите проект:
```bash
mvn clean install

```


4. Запустите:
```bash
mvn spring-boot:run

```



---

## 📡 Системные логи (Docker)

Для мониторинга состояния системы в реальном времени:

```bash
docker logs -f review_platform

```

---

## ⚠️ Устранение неполадок

**Ошибка `duplicate key value violates unique constraint "pk_reviews"`:**
Если вы вставили тестовые данные вручную через SQL или старые версии Liquibase, выполните синхронизацию последовательностей:

```sql
SELECT setval('reviews_id_seq', (SELECT MAX(id) FROM reviews));

```

---

**Status:** `SYSTEM_READY`

**Location:** `http://localhost:8080`

```

---

### Пара советов:
1. **Badges:** Я добавил "бейджи" в начало файла, они делают проект визуально более профессиональным на GitHub.
2. **Лицензия:** Если планируешь выкладывать в открытый доступ, можешь добавить в конец раздел `License: MIT`.



**Хочешь, чтобы я добавил в README описание API-эндпоинтов (Swagger/OpenAPI), если они у тебя есть?**

```
